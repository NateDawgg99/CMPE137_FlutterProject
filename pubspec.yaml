name: fittrainer_planner
description: "Workout planner final project for CMPE 137."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  scoped_model: ^2.0.0
  sqflite: ^2.3.3
  path: ^1.9.0
  flutter_slidable: ^3.1.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true